/// @description Insert description here
// You can write your code in this editor

instance_destroy()
instance_create_layer(x, y, "Buttons", but_close)
instance_create_layer(x, y + 75, "Buttons", but_wolf)
instance_create_layer(x, y + 150, "Buttons", but_sheep)
instance_create_layer(x, y + 225, "Buttons", but_grass)

